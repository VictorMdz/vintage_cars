
      <div id="map"
        data-controller="map"
        data-target="map.map"
        style="width: 50%; height: 100vh; position: sticky; top: 0;"
        data-markers="<%= @markers.to_json %>"
        data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
      </div>

